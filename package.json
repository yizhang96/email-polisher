{
    "name": "email-polisher",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build && cp manifest.json dist/manifest.json",
        "dev": "vite build --watch",
        "clean": "rimraf dist"
    },
    "devDependencies": {
        "@types/chrome": "^0.1.22",
        "@types/node": "^24.7.0",
        "rimraf": "^5.0.5",
        "typescript": "^5.5.4",
        "vite": "^5.4.0"
    }
}
